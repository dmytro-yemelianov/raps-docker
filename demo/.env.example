# RAPS Demo Environment defaults
# These values work with raps-mock — no real APS credentials needed.

APS_CLIENT_ID=mock-client-id
APS_CLIENT_SECRET=mock-client-secret
MOCK_PORT=3000
RAPS_VERSION=latest
